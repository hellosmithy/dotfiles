[user]
  name = Ben Smith
  email = {{ if eq .machine "home" -}}ben@hellosmithy.com{{ else -}}{{ include "private/email.txt" }}{{- end }}

[alias]
  a = add
  c = commit
  cm = commit -m
  co = checkout
  st = status
  p = pull
  P = push
  r = rebase
  undo-commit = reset --soft HEAD^

